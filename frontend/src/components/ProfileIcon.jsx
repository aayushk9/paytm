export function ProfileIcon({userProfile, sendMoneyTo}) {
    return (
        <div>
             {userProfile}
              <div>
                {sendMoneyTo}
              </div>
        </div>
    )
}